<UserControl x:Class="InaManager.View.FormacionesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:InaManager.Converters" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="Transparent">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <converters:BoolToColorConverter x:Key="BoolToColorConverter" />

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#40FFFFFF"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#20FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#30000000"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#80FFFFFF"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid Visibility="{Binding IsListVisible}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="FORMACIONES" FontSize="24" Foreground="White" FontWeight="Bold" FontFamily="Montserrat" VerticalAlignment="Center">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="Black" ShadowDepth="2" Opacity="0.5"/>
                    </TextBlock.Effect>
                </TextBlock>

                <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                        Command="{Binding CreateCommand}"
                        Content="+ NUEVA TÁCTICA"
                        Background="#FFD700" Foreground="Black" FontWeight="Bold" 
                        Padding="15,5" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>

            <Border Grid.Row="1" CornerRadius="15" Background="#15000000" BorderBrush="#30FFFFFF" BorderThickness="1">

                <DataGrid ItemsSource="{Binding Formaciones}" 
                          SelectedItem="{Binding SelectedFormacion}"
                          SelectionMode="Single"
                          IsSynchronizedWithCurrentItem="False"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          HeadersVisibility="Column" Background="Transparent" BorderThickness="0"
                          GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#20FFFFFF"
                          VerticalGridLinesBrush="Transparent" RowBackground="Transparent" RowHeight="65">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="NOMBRE TÁCTICA" Binding="{Binding Nombre}" Width="*" FontWeight="Bold" Foreground="#FFD700"/>

                        <DataGridTemplateColumn Header="ENTRENADOR" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Width="40" Height="40" CornerRadius="20" BorderBrush="#80FFFFFF" BorderThickness="1">
                                        <Border.Background>
                                            <ImageBrush ImageSource="{Binding EntrenadorFoto}" Stretch="UniformToFill"/>
                                        </Border.Background>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="NOMBRE STAFF" Binding="{Binding NombreCompletoEntrenador}" Width="1.5*"/>

                        <DataGridTemplateColumn Header="ACCIONES" Width="260">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">

                                        <Button Content="ACTIVAR" 
                                                Command="{Binding DataContext.AsignarFormacionCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" 
                                                CommandParameter="{Binding Id_formacion}"
                                                Background="#2E7D32" 
                                                Foreground="White" 
                                                BorderThickness="0" 
                                                Padding="10,5" 
                                                Cursor="Hand" 
                                                FontWeight="Bold" 
                                                FontSize="10" 
                                                Margin="0,0,10,0">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="5"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>

                                        <Button Content="DETALLES" 
                                                Command="{Binding DataContext.ShowDetailsCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" 
                                                CommandParameter="{Binding}"
                                                Background="#40FFFFFF" Foreground="White" BorderThickness="0" 
                                                Padding="10,5" Cursor="Hand" FontWeight="Bold" FontSize="10" Margin="0,0,10,0">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="5"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>

                                        <Button Content="X" 
                                                Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" 
                                                CommandParameter="{Binding}"
                                                Background="#B71C1C" Foreground="White" BorderThickness="0" 
                                                Width="25" Height="25" Cursor="Hand" FontWeight="Bold">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="12.5"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <Grid Visibility="{Binding IsDetailVisible}" Background="#E6101010">
            <Button HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20"
                    Command="{Binding BackCommand}"
                    Content="🡠 VOLVER" Background="Transparent" Foreground="White" BorderThickness="0" FontSize="14" FontWeight="Bold" Cursor="Hand"
                    Panel.ZIndex="10"/>

            <Grid Margin="0,60,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="20,0">
                    <TextBlock Text="EDITAR TÁCTICA" Foreground="#FFD700" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

                    <TextBlock Text="Nombre de la Formación" Foreground="Gray" FontSize="12" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding NombreFormacionEdicion, UpdateSourceTrigger=PropertyChanged}" Height="35" FontSize="14"/>

                    <TextBlock Text="Instrucciones:" Foreground="Gray" Margin="0,30,0,10"/>
                    <TextBlock Text="1. Haz clic en una celda vacía para añadir una posición." Foreground="White" TextWrapping="Wrap" Margin="0,0,0,5"/>
                    <TextBlock Text="2. Haz clic en un círculo para quitarlo." Foreground="White" TextWrapping="Wrap" Margin="0,0,0,5"/>
                    <TextBlock Text="3. El portero (PR) es fijo." Foreground="#FFD700" TextWrapping="Wrap"/>

                    <Button Command="{Binding SaveCommand}" Content="GUARDAR CAMBIOS" Margin="0,50,0,0"
                            Background="#2E7D32" Foreground="White" FontWeight="Bold" Height="40" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                </StackPanel>

                <Border Grid.Column="1" Margin="20" CornerRadius="10" BorderBrush="#30FFFFFF" BorderThickness="1" ClipToBounds="True">
                    <Grid>
                        <Image Source="/Images/campo_tactica.png" Stretch="Fill" Opacity="0.8"/>

                        <ItemsControl ItemsSource="{Binding Cuadricula}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="6" Columns="5"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.CellClickCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                            CommandParameter="{Binding}"
                                            Background="Transparent" 
                                            BorderBrush="Black" BorderThickness="0.5">

                                        <Grid>
                                            <Border Width="45" Height="45" CornerRadius="22.5" 
                                                    Background="{Binding IsOccupied, Converter={StaticResource BoolToColorConverter}}" 
                                                    BorderBrush="White" BorderThickness="2"
                                                    Visibility="{Binding IsOccupied, Converter={StaticResource BoolToVis}}">
                                                <TextBlock Text="{Binding DisplayText}" 
                                                           VerticalAlignment="Center" HorizontalAlignment="Center" 
                                                           Foreground="Black" FontWeight="Bold" FontSize="14"/>
                                            </Border>
                                        </Grid>

                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" 
                                                        BorderBrush="{TemplateBinding BorderBrush}" 
                                                        BorderThickness="{TemplateBinding BorderThickness}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#20FFFFFF"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>